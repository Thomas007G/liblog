import React from "react";
import styled from "styled-components";

const Header = styled.div`
    height: 8vh;
    width: 100%;
    position: fixed;
    top: 0;
`

export default () => {
    return (
        <Header>
            brueh
        </Header> 

    );
}